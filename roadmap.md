# Required

## UX improvements

[] menu bar during emulation (pause, reset, open ROM, fast-forward toggle)
[] prompt user about default settings on first launch (palette, volume, key bindings)
[] configurable directory for save files and screenshots in settings

## Accuracy

[] blargg halt_bug — halt instruction edge case, affects some real games
[] blargg mem_timing-2 — extended memory timing coverage

## Features

[] save states following the BESS specification
[] drag-and-drop ROM loading — users expect to drop a .gb/.gbc file onto the window

# Future

[] mooneye test suite — comprehensive hardware accuracy (many edge cases)
[] enhanced pokemon features — party viewer, speedrun helper, etc.
[] configurable DMG palette presets
